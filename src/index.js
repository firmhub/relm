// Re-export everything
import {
  component
} from './component';

import {
  joinArgs,
  createDispatcher,
  createStore,
} from './state';

import {
  updateStrategy,
  updateNested,
} from './updaters';

import {
  addStyles,
} from './stylesheet';

export {
  component,
  joinArgs,
  createDispatcher,
  createStore,
  updateStrategy,
  updateNested,
  addStyles,
};

export default {
  component,
  joinArgs,
  createDispatcher,
  createStore,
  updateStrategy,
  updateNested,
  addStyles,
};
